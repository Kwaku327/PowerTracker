"""
PowerTrack Streamlit application package.

This file makes the directory a proper Python package so that modules such as
`liftingcast_loader` and `scoring` can be imported when the app is executed
from different working directories (e.g., when launched via the top-level
entry point).
"""

